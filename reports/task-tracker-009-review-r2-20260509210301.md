# Sub-agent実行レポート

## タスク

- 目的: `TRACKER-009` follow-up 後の task 単位レビューを行い、ball tracking / camera 間 merge / identity continuity / primary ball 選定 / secondary stable sort の実装と対応する contract 更新の妥当性を再確認する
- タスク種別: review

## sub-agentを使う理由

- 理由: `review-enforcer` により task 完了前の dedicated review は mandatory sub-agent work であるため

## 対象範囲

- 対象: `TRACKER-009` の follow-up 差分、およびそれが触れた `Tracker.Core` / `Tracker.Tests` / tracking files

## 対象外

- 対象外: `.gitignore` の既存変更、`TRACKER-010` 以降の未実装 task、legacy report 未追跡ファイル

## 実行コマンド

- 実行コマンド:
  - `sed -n '1,220p' /home/ibis/AI/CodexSkill/skills/review-enforcer/SKILL.md`
  - `sed -n '1,260p' /home/ibis/AI/CodexSkill/skills/sub-agent-task-manager/SKILL.md`
  - `sed -n '1,260p' reports/task-tracker-009-review-r2-20260509210301.md`
  - `git diff -- Tracker/Tracker.Core/TrackerExecutionContracts.cs Tracker/Tracker.Tests/Contracts/TrackerEngineTemporalContractTests.cs Tracker/Tracker.Core/Design/tasks-status.md Tracker/Tracker.Core/Design/phases-status.md`
  - `sed -n '1,1040p' Tracker/Tracker.Core/TrackerExecutionContracts.cs`
  - `sed -n '300,860p' Tracker/Tracker.Tests/Contracts/TrackerEngineTemporalContractTests.cs`
  - `nl -ba Tracker/Tracker.Core/TrackerExecutionContracts.cs | sed -n '320,620p'`
  - `dotnet test Tracker/Tracker.Tests/Tracker.Tests.csproj --filter FullyQualifiedName~TrackerEngineTemporalContractTests`  (`Passed: 27, Failed: 0`)

## 対象ファイル

- 変更または確認したファイル:
  - `Tracker/Tracker.Core/TrackerExecutionContracts.cs`
  - `Tracker/Tracker.Tests/Contracts/TrackerEngineTemporalContractTests.cs`
  - `Tracker/Tracker.Core/Design/tasks-status.md`
  - `Tracker/Tracker.Core/Design/phases-status.md`
  - `reports/task-tracker-009-review-r2-20260509210301.md`

## 指摘事項

- blocking normal-path problems:
  - `Tracker/Tracker.Core/TrackerExecutionContracts.cs:348-395` `availableTracks` が camera ごとに loop 開始時の snapshot 固定で、同じ committed group 内で後続 observation に対して更新されません。さらに `matchedTrackIds` により 1 group 中 1 回しか既存 track を再利用できないため、`mergeWindowNs` 内に同一 camera の連続 frame が入る normal path で、同じ ball の連続観測が別々の local track として増殖します。結果として 1 つの実ボールが複数 `TrackedBallState` に分裂し、phantom secondary ball と誤った `PrimaryBallTrackId` を生成します。追加テストは各 committed group で camera ごとの ball observation を 1 回までしか通しておらず、この回帰を捕捉していません。
- user-confirmation-required gaps:
  - なし
- non-blocking held concerns:
  - なし
- 指摘要約または「指摘なし」:
  - blocking 1 件。same-camera の複数 detection が 1 committed group に入るケースで ball track が重複生成され、primary/secondary ball の normal path を壊します。

## 結果

- 結果:
  - follow-up 差分の再レビューを実施し、blocking finding 1 件を記録しました。`TrackerEngineTemporalContractTests` は現状すべて通過しましたが、上記シナリオは未検証のままです。

## リスク

- 未解決のリスクまたは後続対応:
  - `mergeWindowNs` が camera frame interval 以上の設定では、この不具合が通常運用で発火し得ます。修正時は same-camera / same-group の連続 observation を 1 track に畳み込む回帰テストを追加して再レビューする必要があります。
