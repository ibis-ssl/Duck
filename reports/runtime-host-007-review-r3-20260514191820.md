# RUNTIME-HOST-007 r3 レビュー

## Blocking findings

- Blocking findings なし。

## Non-blocking findings

- Non-blocking findings なし。

## Tests reviewed

- 追加修正 report `reports/runtime-host-007-configurable-sample-interval-20260514191628.md` の証跡として、`VisionReceiverConfigurationResolverTests|DiagnosticsSample` 17 passed、R007 影響範囲 98 passed、`Tracker.DebugHost` build success、`Tracker.Tests` build success、`git diff --check` success を確認した。
- 親方針に従い、追加の `dotnet test` / `dotnet build` は実行していない。
- `Tracker/Tracker.Tests/VisionReceiverConfigurationResolverTests.cs:189` から `:222` で、設定値 25ms を使うことと、0 / -1 が `VisionPacketCaptureOptions.DefaultDiagnosticsSampleIntervalMilliseconds` へ fallback することを検証している。

## Serena 使用有無

- Serena 使用あり。開始時に `initial_instructions` を読み、`/home/ibis/ssl/IbisDuck` を activate した。
- `check_onboarding_performed` は未実施状態を返したが、今回の作業は read-only review であり、memory 作成は行っていない。
- コード調査では `get_symbols_overview`、`find_symbol`、`search_for_pattern` で `VisionPacketCaptureOptions`、`DiagnosticsSampleHostedService`、diagnostics sample 関連の参照を確認し、必要な行番号確認だけ shell の `nl -ba` を併用した。

## 確認事項

- `DiagnosticsSampleHostedService` は constructor で `VisionReceiverOptions.PacketCapture` から interval を解決し、`ExecuteAsync` の `PeriodicTimer` へ渡している。`ResolveSampleInterval` は `DiagnosticsSampleIntervalMilliseconds` を読み、0 以下なら `DefaultDiagnosticsSampleIntervalMilliseconds` へ戻すため、invalid 設定で通常 path は停止しない。該当箇所: `Tracker/Tracker.DebugHost/Tracking/DiagnosticsSampleHostedService.cs:17`、`:28`、`:31`、`:42`。
- `VisionReceiver:PacketCapture:DiagnosticsSampleIntervalMilliseconds` は options / appsettings / README に揃っている。該当箇所: `Tracker/Tracker.DebugHost/Vision/VisionReceiverOptions.cs:19`、`:29`、`Tracker/Tracker.DebugHost/appsettings.json:6`、`:11`、`Tracker/Tracker.DebugHost/README.md:140`、`:146`、`:150`。
- design docs は、UI render tick が diagnostics logging cadence を決めず、diagnostics sample tick が設定値で動くことを明記している。該当箇所: `Tracker/Design/DebugHost/raw-vision-viewer-plan.md:256` から `:262`、`Tracker/Design/RuntimeHost/runtime-host-plan.md:94` から `:98`。
- `diagnostics-samples.jsonl` は sidecar contract 名として固定のまま維持されている。これは運用 tuning 値ではなく replay / metadata contract なので、無理に設定化していない判断は妥当。該当箇所: `Tracker/Tracker.DebugHost/Vision/VisionPacketCaptureFile.cs:103` から `:105`、`Tracker/Design/DebugHost/raw-vision-viewer-plan.md:389`。
- R007 r2 で閉じた regression 観点は維持されている。`DiagnosticsSampleHostedService` は DI 登録済みで Home UI 非依存に動く。該当箇所: `Tracker/Tracker.DebugHost/Program.cs:60` から `:62`。`Home.razor` の CaptureOff は packet / render writer を止めるだけで diagnostics sample writer を直接操作しない。該当箇所: `Tracker/Tracker.DebugHost/Components/Pages/Home.razor:417` から `:426`。`DiagnosticsSampleLogWriter.Stop()` は writer 状態だけを解放し、shared `VisionPacketCaptureSession` 全体を停止しない。該当箇所: `Tracker/Tracker.DebugHost/Tracking/DiagnosticsSampleLog.cs:319` から `:330`。
- Diagnostics UI は diagnostics sample sidecar を優先し、sample sidecar がある場合は field source options / timeline をそこから構築する。旧 render snapshot fallback を通常経路として復活させた差分は確認できなかった。該当箇所: `Tracker/Tracker.DebugHost/Tracking/TrackerDiagnosticsComparisonViewStateReader.cs:105` から `:143`。

## Final outcome

- 判定: Pass。
- Follow-up required: なし。
- r2 の non-blocking hold だった sample interval 固定値は、今回の設定化差分で解消された。
- 残リスク: 今回は worker report の証跡を review 入力として扱い、親方針どおり追加 test は実行していない。Home の live display refresh tick は残っているが、diagnostics sample capture / logging cadence からは分離済みであり、R007 の blocker ではない。
