# DOC-LINT-003 履歴本文と出現箇所台帳の確認

## 対象と役割

記録時刻: 2026-09-17T07:28:40.604551+09:00。作業担当の自己点検であり、独立最終レビューではない。技術的な親HEADは `162f4894d8ca4dbd6b69828f9a359181a1bf35fb`。下記の本文変更を含む内容はファイル別SHA-256で識別し、これから作るコミット自身のSHAを先取りしない。

元の作業ツリーでは別担当の編集・pushを確認したため、`work/wording-ledger-20260917-0709` と専用作業ツリーで分離した。他担当の未コミット差分・未追跡ファイル・プロセスは変更していない。公開された診断設計、Core構成設計、表示設計、DebugHost利用手順は安全に取り込んだ。

## 本文の確認と修正

履歴4文書の原文と現在の本文を読み比べ、Coreの25作業、DebugHostの21作業について、条件・数量・否定・対象外・保留・初回から修正までの経過・詳細レポートを確認した。旧作業を今回の完了条件として流用せず、未着手を実装済みにはしていない。

Core履歴の2箇所では、入力元別の利用中トラッカーを取得するAPIの扱いと、同じUUIDが衝突する場合の扱いを、後続作業で検討する二つの事項として自然に並べた。

DebugHostのRAW-VISION-016では、参照先の最終検証・最終レビューを読み、本文に抜けていた実画面確認の制約を補った。対象37件とビルド・画面応答の成功は、重ね合わせ表示の実操作やlatest-before情報の実画面確認の成功ではない。当時の最終レビューはこの制約を残るリスクとして保持していた。初回の確認用サーバー停止未確認と、後のレビューで通信ポート18160の待ち受けがなかった確認も分けて記録した。現在のプロセス状態についての主張ではない。

`229件成功・1件失敗`、`238件成功・1件失敗`、`237件成功・1件失敗`、`240件成功・1件失敗`を保持した。RAW-VISION-014の37件中26件成功・11件失敗と、後続実装の37件成功も別に扱った。

## 対照台帳

`diagnostics/wording-ledger-20260917-0709/occurrence-ledger-archive.jsonl` に、4,336出現箇所のうち履歴4文書の939件を収録した。原出現ID、許可語と実表記、元SHA・ファイル・行・列、最終位置、採否、理由、判断記録への参照を持つ。

元の184行と最終表現・前後の説明は `archive-line-correspondence.json` に保持する。一行の履歴を作業一覧と詳細本文へ分割した場合は両方の位置を残す。共通理由は `archive-semantic-review.json` の作業別記録を参照し、語の一致だけで自動承認していない。939件以外の3,397件は、この台帳の確認済み件数には含めない。

基準時点の全4,336件は、原文の実際の行・列・表記と一致した。診断設計90差分とCore構成設計247差分の既存対応記録は、保存ハッシュと最終本文が一致した。この構造検証だけを、新たな文脈確認や独立レビューの合格には扱わない。

履歴の旧台帳で44件の範囲が一致しなかった原因は、末尾空行を範囲に含めていたことだった。本文欠落ではない。今回の対照表では最終の範囲と実本文を併記する。最初の検出記録と、その後の確認記録は別名で保持した。

## 検証

履歴4文書のtextlint、cspell、whitelist、および差分空白検査は、`validation-ledger-archive-r2-20260917-0723.json` で終了値0。初回は通信ポートと番号の境界でwhitelistが1件失敗したため、その記録も残し、許可済みの複合語と番号の間に空白を入れて再検証した。単独の「ポート」を新たに許可していない。

全19文書の `npm run lint:md` は終了値123で未通過。後段も個別に実行し、全体textlintは0、cspellは引用中のexec 1件で123、whitelistは同じexec 1件と引用中のサブエージェント2件で1だった。poststages-0728.jsonに結果を保持する。既知の引用問題を成功扱いにしていない。

元発言2件は用語整理前のSHAと一致し、`reports/history/` の4原文保存版はバイト単位で一致した。306項目の許可一覧、検査設定、共有検査器、製品コードを変更していない。直接引用の方針は承認済みだが、この担当が使う固定済み検査器には例外処理がない。

Node・npm・Python・依存版、設定・対象ハッシュはintake.jsonとログに保存した。診断ログはcheckpoint-logs.tar.gzと各validation単位のtarに保存し、stdout・stderr・終了値を含む。依存物はchecker-manifest.jsonの全ファイルハッシュと一致する専用コピーを使った。

## 残件とCIの境界

全体の対照台帳、現行進捗文書の同期、PR本文・最終コメントの同期、公開後のcurrent HEAD一致CI確認は別の作業として継続する。この記録は全件確認済み、全体lint成功、PR完了を意味しない。

確認した.NETの診断workflowはmain側にあり、PRのHEADツリーにはない。失敗時はTRX・stdout・stderr・診断ログ・ソース等を保存するが、Markdown CIや成功時のartifactではない。別SHAのworkflowや成功runをこの本文のMarkdown検証の証拠には使わない。

新たな用語の承認を要する変更はない。キャプチャの許可判定と承認済み直接引用の例外はCodexSkill側の別作業であり、Duck本文の確認を止める理由にはしない。
