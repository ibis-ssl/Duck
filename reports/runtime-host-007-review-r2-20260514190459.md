# RUNTIME-HOST-007 r2 レビュー

## Blocking findings
- Blocking findings なし。

## Non-blocking findings
1. `Tracker/Tracker.DebugHost/Tracking/DiagnosticsSampleHostedService.cs:8` / `Tracker/Design/DebugHost/raw-vision-viewer-plan.md:262`  
   diagnostics sample loop は UI から独立した hosted service へ移っており前回 blocker は解消している。一方、sample interval は `100ms` 固定で、`raw-vision-viewer-plan.md` の「raw snapshot cadence を失わない保存境界」を文字どおり読むと、raw SSL-Vision が 10Hz より高い場合に全 raw update を sample sidecar に残す保証はまだない。R007 の focused criteria は「UI render tick 依存を外す」「R003 contract を green にする」ため、今回の normal-path blocker にはしないが、raw cadence を lossless に扱う必要があるなら別途 task 化または設計文の明確化が必要。hold として記録する。

## Tests reviewed
- 追加テスト実行はユーザー指示どおり行っていない。
- review-fix report の evidence:
  - `RuntimeHostDiagnosticsSampleBoundaryContractTests|DiagnosticsSample`: 8 passed。
  - `TrackerDiagnosticsComparisonViewStateTests|VisionPacketCaptureTests|DiagnosticsFieldViewFactoryTests|DiagnosticsPlaybackStateTests`: 84 passed。
  - `Tracker.DebugHost` build: 0 warnings / 0 errors。
  - `Tracker.Tests` build: 0 warnings / 0 errors。
  - `git diff --check`: success。
- `reports/runtime-host-007-verification-20260514184527.md` は初回 fix 前の 4 passed / 82 passed evidence のままだが、今回の r2 判定では `reports/runtime-host-007-review-fix-20260514185807.md` の 8 passed / 84 passed evidence を採用した。
- tests coverage の確認:
  - `VisionPacketCaptureTests.DiagnosticsSampleCaptureLoop_WhenCaptureEnabled_WritesSamplesWithoutHomeUi` が Home UI を介さず `DiagnosticsSampleCaptureLoop.CaptureOnce()` で sample sidecar を保存できることを固定している。
  - `VisionPacketCaptureTests.DiagnosticsSampleLogWriterStop_DoesNotStopSharedCaptureSession` が writer stop と shared session stop の分離を固定している。
  - `RuntimeHostDiagnosticsSampleBoundaryContractTests.UiState_ForVisionInputAndIbisTrackerLoadsDiagnosticsSampleFrames` が UI state で Vision Input / ibis tracker も sample sidecar frame を reader から読むことを固定している。
  - `RuntimeHostDiagnosticsSampleBoundaryContractTests.LoadFieldSourceFrame_ForVisionInputWithoutDiagnosticsSampleDoesNotFallbackToRenderSnapshot` が旧 render snapshot fallback を復活させないことを固定している。

## Serena 使用有無
- Serena 使用: あり。
- `initial_instructions` を読了し、`/home/ibis/ssl/IbisDuck` を `activate_project` 済み。
- `check_onboarding_performed` は未実施を返した。`onboarding` は instructions 取得まで実行したが、今回の task は read-only review であり memory 作成は行っていない。
- symbolic tools 使用:
  - `get_symbols_overview`: `DiagnosticsSampleCaptureLoop.cs`、`DiagnosticsSampleHostedService.cs`、`DiagnosticsSampleLog.cs`
  - `find_symbol`: `DiagnosticsSampleCaptureLoop`、`DiagnosticsSampleHostedService`、`DiagnosticsSampleLogWriter`、`DiagnosticsSampleRecord.FromRenderSnapshot`、`TrackerDiagnosticsComparisonUiState.Load`、`RefreshFieldSourceFrames`、`LoadTrackerFieldSourceFrame`、`NeedsTrackerFieldSourceFrame`、`TrackerDiagnosticsComparisonViewStateReader.LoadFieldSourceFrame`、`CreateDiagnosticsSampleFieldSourceFrame`、`Diagnostics.CreateFieldRenderModel`、`DiagnosticsFieldOverlayRenderModelFactory.CreateFieldRenderModel`
  - `search_for_pattern`: sample interval / hosted service registration / old render snapshot fallback / `WorldFrameCommitted` usage

## 確認事項
- Blocking 1 の解消:
  - `Home.razor` から `DiagnosticsSampleLogWriter` injection と `CaptureSample()` 呼び出しは除去されている。
  - `Program.cs:60` から `DiagnosticsSampleLogWriter`、`DiagnosticsSampleCaptureLoop`、`DiagnosticsSampleHostedService` が登録され、`DiagnosticsSampleHostedService.ExecuteAsync()` が UI 非依存に `captureLoop.CaptureOnce()` を呼ぶ。
  - `DiagnosticsSampleCaptureLoop.CaptureOnce()` は CaptureOn の場合だけ `VisionLiveDisplaySnapshotProvider.CaptureRenderTickSnapshot()` を使い、CaptureOff では writer handle のみ停止する。
- Blocking 2 の解消:
  - `TrackerDiagnosticsComparisonUiState.NeedsTrackerFieldSourceFrame()` は `VisionInput` / `IbisTracker` も reader frame を必要とする source として扱う。
  - `TrackerDiagnosticsComparisonViewStateReader.LoadFieldSourceFrame()` は `VisionInput` / `IbisTracker` で diagnostics sample sidecar が無い場合に `SidecarUnavailable` を返し、旧 render snapshot fallback へ戻らない。
  - `Diagnostics.razor.cs` と `DiagnosticsFieldOverlayRenderModelFactory` は Vision Input / ibis tracker の balls / robots を `TrackerDiagnosticsFieldSourceFrame.SemanticSummary` から作る。
- Stop ownership:
  - `DiagnosticsSampleLogWriter.Stop()` は writer handle と writer-local counters の reset のみを行い、`VisionPacketCaptureSession.Stop()` を呼ばない。
  - CaptureOff の shared session stop は既存の packet/render writer stop 経路に残っている。
- 設計整合:
  - `raw-vision-viewer-plan.md` と `runtime-host-plan.md` は UI render tick ではなく hosted service の diagnostics sample loop を通常経路として記述している。
  - 旧 render snapshot sidecar だけの session は unsupported / degraded legacy として扱う方針が維持されている。

## Final outcome
- Pass。前回 blocking findings 2 件は解消済み。
- follow-up required なし。
- non-blocking hold: fixed 100ms sample interval と raw snapshot cadence guarantee の関係は、必要なら後続で設計明確化する。
