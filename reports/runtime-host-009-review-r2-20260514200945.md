# RUNTIME-HOST-009 r2 レビュー

## Blocking findings

- Blocking findings なし。

## Non-blocking findings

- Non-blocking findings なし。
- Hold 継続: latest packet buffer は latest 優先のままでよい。R009 の性能優先 normal path と設計に合っており、queueing 必須とは判断しない。実機 cadence / drop の扱いは R010 manual evidence 後に必要なら判断する。

## Tests reviewed

- 初回 review report `reports/runtime-host-009-review-20260514195653.md`、review-fix report `reports/runtime-host-009-review-fix-20260514200105.md`、implementation report `reports/runtime-host-009-implementation-20260514194405.md` を確認した。
- review-fix validation は `RuntimeHostOperationLoopTests` 5 passed、`Tracker.RuntimeHost` build pass、`Tracker.Tests` build pass、`git diff --check` pass。親方針どおり追加の `dotnet test` / `dotnet build` は実行していない。
- 初回 implementation evidence の R009 focused 3 passed、broad focused 26 passed / 1 failed、adjusted focused 26 passed は継続証跡として扱う。残る broad focused failure は既存 out-of-scope の DebugHost ownership assertion であり、R009 r2 completion evidence の blocker にはしない。
- missing active profile regression は `RuntimeHostTrackerOptions_WithMissingActiveProfile_Throws` と `RuntimeHostTrackerOptions_WithBlankActiveProfileAndMissingDefaultProfile_Throws` で固定されている。該当箇所: `Tracker/Tracker.Tests/RuntimeHostOperationLoopTests.cs:64` から `:96`。

## Serena 使用有無

- Serena 使用あり。開始時に `initial_instructions` を読み、`/home/ibis/ssl/IbisDuck` を activate した。
- `find_symbol` で `RuntimeTrackerConfigurationResolver` と `RuntimeHostOperationLoopTests` を確認した。`search_for_pattern` で test XML summary / `[Fact]` 配置を確認した。未コミット差分と行番号確認には shell の `git status` / `nl -ba` / `rg` を併用した。

## 確認事項

- 初回 blocking finding は解消している。`RuntimeTrackerConfigurationResolver.Resolve` は blank `ActiveProfileName` を `default` に正規化するが、`ResolveProfile` は requested profile が `Profiles` に存在しない場合に `default` fallback せず、`Tracker active profile '<name>' was not found in Tracker:Profiles.` の `InvalidOperationException` を投げる。該当箇所: `Tracker/Tracker.RuntimeHost/RuntimeTrackerConfigurationResolver.cs:17` から `:20`、`:50` から `:60`。
- `Tracker:ActiveProfileName=missing` は DI から `TrackerRuntimeResolvedOptions` を解決する時点で `missing` を含む例外になる。該当 test: `Tracker/Tracker.Tests/RuntimeHostOperationLoopTests.cs:64` から `:78`。
- blank active profile は `default` 扱いを維持し、`default` profile が存在しない場合は `default` を含む例外になる。該当 test: `Tracker/Tracker.Tests/RuntimeHostOperationLoopTests.cs:81` から `:96`。
- R009 未コミット差分全体について、RuntimeHost project 内に DebugHost / Blazor / diagnostics / capture / replay への参照は見つからなかった。R009 normal path、operation loop interval、receiver、publish / snapshot path に関する初回 review の確認事項を崩す追加変更は確認していない。
- XML documentation / coding standard 上の追加 blocker は見つからなかった。追加された `[Fact]` 2 件は XML summary を持っている。該当箇所: `Tracker/Tracker.Tests/RuntimeHostOperationLoopTests.cs:64` から `:67`、`:81` から `:84`。

## Final outcome

- 判定: Pass。
- Follow-up required: なし。
- 初回 blocking finding は解消済み。初回 review の non-blocking hold は R010 manual evidence 後判断として継続する。
