# Progress Sync Report

## Task

`TRACKER-054` live tracker receiver の endpoint override を追加する。

## Reason

ユーザーが実運用で 3rd party tracker が記録されないことを確認し、receiver は ibis publish endpoint と同じ設定を既定にしつつ、別 endpoint を明示指定できる設定口も必要だと判断したため。

## Updated Files

- `Tracker/Tracker.Core/Design/tasks-status.md`
- `Tracker/Tracker.Core/Design/phases-status.md`

## Synchronized State

- `TRACKER-054` を PR ready 前の current task として追加した。
- `TRACKER-053` は `TRACKER-054` 完了後の PR ready 化タスクに戻した。
- `reports/tracker-port-settings-investigation-20260512230600.md` の調査結果を前提に、既定 endpoint と explicit receive override を exit criteria に入れた。

## Notes

- `TRACKER-054` は既存固定一覧に対するユーザー明示追加要件であり、`TRACKER-055` 以降の hardening を無制限に開くものではない。
- runtime profile switch 後の receiver socket 再構成は今回の最小変更には含めず、起動時 endpoint 固定として README / design に明記する。
