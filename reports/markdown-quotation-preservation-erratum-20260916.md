# 直接引用の保全に関する訂正と追加修正

## 訂正

用語整理前の基準は `f5482ab85d49832a21ef6029d6aa3354f6c7c4f4`。`feedback-points/feedback-points.md` の直接引用は2件あり、前回の修正では FP-001 だけが原文に戻っていた。FP-002 の原文は「サブエージェントにcodex exec実行させるのやめろ」だが、開始時点の `a2e63f05345bb4301d6033273910a26ccfb4a1d2` では「委譲先に入れ子実行させるのやめろ」へ変わったままだった。今回、FP-002 も原文へ戻した。

前回の `history-preservation-final.json` が示す開始時点とのバイト一致は、用語整理前の原文との一致を意味しない。直接引用の保全をすべて確認したかのような説明は不十分だった。以前の検証記録は変更せず、本書を訂正記録として追加する。

## 変更と検証

引用外の要約と次対応は、エージェント・レポート・ツール・作業ディレクトリ・レビューなどの許可済み表現で自然な文章へ修正した。記録起点、分類、重複識別子、回数、関連スキル、状態、日付は変えていない。新規の指摘追加やスキル変更ではなく、Duck の既存履歴の原文保全であるため、CodexSkill 側の台帳・スキル・Issue は変更しない。

`tools/lint/README.md` は、対象が Markdown であることを明記し、日本語へ置換することだけを促す説明を修正した。許可済み名称と引用を保ち、登録・検査条件の変更前に利用者の承認を受ける方針を記載した。検査設定や除外対象は変更していない。

対象の textlint と `git diff --check` は成功。cspell は引用中の `exec` 1件で失敗し、whitelist 検査は `exec` 1件と「サブエージェント」2件で失敗した。全19文書の `npm run lint:md` は cspell で停止し、終了値123。後段のwhitelist検査が未実行であることを成功として扱わず、対象を直接指定したwhitelist検査の失敗も別に記録した。

検証結果、標準出力、標準エラー、終了値は `reports/diagnostics/wording-occurrence-audit-20260916/validation-guide-feedback.json` と同ディレクトリの `guide-feedback-validation-logs.tar.gz` に保存する。用語整理前との2引用の一致と台帳の固定項目は `check_quotation_preservation.py` で検証し、結果を `quotation-preservation.json` に保存する。

## 承認待ちの扱い

引用の原文は改変せず、`exec` や「サブエージェント」を全域の許可語へ追加しない。提案は、出典と引用範囲を明示し、原文との対応を確認できる直接引用だけについて、cspell とwhitelist検査の例外を設ける方法である。かぎ括弧内やMarkdown引用を一律に除外する方法、文書全体を検査対象外にする方法は採らない。

この提案は未承認・未実装であり、今回承認された「単体テスト」「相対パス」「コミット」の3語に含まれるとは扱わない。引用に伴うlint失敗は未解決のまま明示する。
