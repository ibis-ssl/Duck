# Sub-agent実行レポート

## タスク

- 目的: `TRACKER-010` の task 単位レビューを行い、kick と contact metadata 実装および対応する contract 更新の妥当性を確認する
- タスク種別: review

## sub-agentを使う理由

- 理由: ユーザーが `review-only`、`Do not spawn agents`、`do not run codex exec`、`do not re-enter development-orchestrator` を明示しているため、sub-agent は使用せず親エージェントが task-scoped review を実施した。

## 対象範囲

- 対象: `TRACKER-010` の差分、およびそれが触れた `Tracker.Core` / `Tracker.Tests` / tracking files

## 対象外

- 対象外: `.gitignore` の既存変更、`TRACKER-011` 以降の未実装 task、legacy report 未追跡ファイル

## 実行コマンド

- 実行コマンド: `git status --short -- Tracker/Tracker.Core/TrackerExecutionContracts.cs Tracker/Tracker.Tests/Contracts/TrackerEngineTemporalContractTests.cs Tracker/Tracker.Core/Design/tasks-status.md Tracker/Tracker.Core/Design/phases-status.md .gitignore reports`、`git diff -- Tracker/Tracker.Core/TrackerExecutionContracts.cs Tracker/Tracker.Tests/Contracts/TrackerEngineTemporalContractTests.cs Tracker/Tracker.Core/Design/tasks-status.md Tracker/Tracker.Core/Design/phases-status.md`、`rg -n "TRACKER-010|kick|contact metadata|KickEventState|BallContactState|LatestContact|KickedBall|HasBallContact|KickDetected|ContactChanged" ...`、`sed -n ...`、`nl -ba ...`

## 対象ファイル

- 変更または確認したファイル: `Tracker/Tracker.Core/TrackerExecutionContracts.cs`、`Tracker/Tracker.Tests/Contracts/TrackerEngineTemporalContractTests.cs`、`Tracker/Tracker.Core/TrackerModelContracts.cs`、`Tracker/Tracker.Tests/Contracts/TrackerCoreContractSurfaceTests.cs`、`Tracker/Tracker.Tests/Contracts/TrackerContractFixture.cs`、`Tracker/Tracker.Core/Design/tasks-status.md`、`Tracker/Tracker.Core/Design/phases-status.md`、`Tracker/Tracker.Core/Design/tracker-architecture-plan.md`

## 指摘事項

- blocking normal-path problems: 1. `Tracker/Tracker.Core/TrackerExecutionContracts.cs:224-234`, `285-333`, `431-459` では `latestContactState` を ball track に結び付けずにグローバル再利用しているため、primary ball が別 `InternalTrackId` へ切り替わった frame でも、前の ball の `LastRobotId` が新しい primary ball に引き継がれます。その結果、multi-ball 条件で ball A の最終接触者が ball B の `LatestContact` / `KickDetected` に誤帰属します。今回追加されたテストは単一 ball 直線ケースだけで、primary ball 切替を一度も通していません（`Tracker/Tracker.Tests/Contracts/TrackerEngineTemporalContractTests.cs:1152-1250`）。 2. `Tracker/Tracker.Core/TrackerExecutionContracts.cs:480` は `primaryBall.VZMmPerS > 0` だけで `KickKind = "chip"` にしており、設計書の「`z` または `vz` の上昇が閾値以上なら chip 候補」という契約から外れています。わずかな正の Z 速度ノイズでも flat kick が chip 扱いになり得ますが、追加テストは `VZMmPerS == 0` の flat 例しか固定していません（`Tracker/Tracker.Tests/Contracts/TrackerEngineTemporalContractTests.cs:1216-1250`）。
- user-confirmation-required gaps: なし。
- non-blocking held concerns: `Tracker/Tracker.Core/TrackerExecutionContracts.cs:15-22` の kick/contact 閾値はまだ固定値で、`TrackerKickDetectorOverrides` の調整余地を使っていない。現タスクの exit criteria 自体は満たし得るため直ちに block しないが、false positive / false negative の現場調整は後続 task までできない点は保持リスク。
- 指摘要約または「指摘なし」: blocking 2 件。multi-ball での stale contact / kick 誤帰属と、chip 判定の閾値未適用により、TRACKER-010 は現状のまま完了扱いにすべきではない。

## 結果

- 結果: fail。task-scoped review は実施済みだが blocking normal-path problems が 2 件残っているため、修正と再レビューが必要。

## リスク

- 未解決のリスクまたは後続対応: primary ball 切替を含む multi-ball temporal regression test と、`VZMmPerS` が小さく正の flat kick regression test を追加してから再レビューすること。
