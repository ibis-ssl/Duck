# 文書検査手順の表記修正報告

## 状態と対象

対象は ibis-ssl/Duck PR #20、DOC-LINT-003 の指摘対応。今回の文書修正は `tools/lint/README.md` に限定した。PR全体の完了や独立レビュー合格を宣言する報告ではない。

開始時に確認したPR HEADは `9e48670560a17feb4fd65f126a6c7eac7116f7dc`。別の実行元が公開した本文11文書の変更 `84dbf6c235be9e89e775bc0020fb891d56131ee5` を取り込み、手順書の変更を `8571c7b7bdf940414375cfb378e3a35a37eba224` としてcommit/pushした。PRブランチは `docs/runtimehost-readme-appsettings`、baseは `main`。元レビュー対象は `42e063760f90987262e397b43ba084c9ee60b944`。

この報告と引き継ぎは後続の報告コミットとして保存する。最終公開HEADと、そのHEADに一致するCI結果はPRコメントに記録する。本文作成時点では報告コミット・そのpush・そのHEADのCI確認は未完了であり、将来のSHAを記入しない。

## 作業分離

08:24 JST、RDCの同一セッション PID 609572 に、このチャットから送っていない本文編集命令があることを履歴で確認した。その後、このセッションへの命令送信を止めた。実行元のエージェントの識別情報は確認できていない。

`/home/ibis/.local/share/duck-lint-guide-clarity-20260916-0826-c7f2` を開始時のPR HEADから別に作成し、以後は短時間で終了する独立したプロセスを使った。本文側の作業ツリーをreset、stash、clean、終了していない。この報告の正式な検証は、この新しい作業ツリーで採取した結果だけを使用する。

端末は `ibis-ThinkBook-14-G7-IML`、machine IDは `75840a70-dea0-4d8c-9bb4-fe97c78bc11e`、shellは `/bin/bash`。作業ブランチは `work/lint-guide-clarity-20260916-0826-c7f2`。Git操作はRDC、リモートのHEAD確認とPR操作はGitHub connectorを使用した。

## 変更内容

| 変更前 | 変更後・意図 |
| --- | --- |
| 作業一式 | リポジトリ。対象がGitで管理するリポジトリであることを明記した。 |
| 委譲先の点検 | エージェントのレビュー。既存の許可語を使い、ユーザー承認が必要な条件は維持した。 |
| 構築時の分離、構築用補助パッケージ | ビルド時の環境分離、ビルド用の補助パッケージ。コマンドや環境変数は変えていない。 |
| 記号参照 | `ln -s` で作成するリンクとして説明し、参照先とGit管理対象外という条件を維持した。 |
| 構築木 | ビルド用ディレクトリ。除外対象のパスは変えていない。 |
| 囲み付き符号片、逆引用符で囲んだ行内の識別子 | 行内の識別子と複数行のソースコードの説明へ変更。通常の文章を囲んで検査を回避することは禁止したままにした。 |
| 連結符付き表記 | `-` でつないだ表記。別表記の追加には既存どおり明示登録が必要である。 |

文書差分は18行の置き換え。Bashの囲み内コマンドと見出しは変更前と一致する。YAML例の説明文にある「作業一式」も「リポジトリ」へ直したが、設定キーや許可登録の構造は変えていない。許可一覧、表記揺れ辞書、対象除外設定、package設定、製品ソースは変更していない。

## 検証結果

| 検証 | 結果 |
| --- | --- |
| 変更前HEADの全19文書 `npm run lint:md` | 終了値1。原文引用中の「サブエージェント」1件。 |
| 修正後の手順書のtextlint / cspell / 許可一覧検査 | すべて終了値0。 |
| 別の実行元の変更を取り込んだ技術HEADでの全体検査 | 終了値1。textlint成功、cspell 19文書・0件、原文引用中の「サブエージェント」1件。 |
| 差分の空白検査 | 終了値0。 |
| コマンド例・見出し・編集範囲・本文ハッシュなどの補助確認 | 5項目成功。製品のテスト件数ではない。 |
| ローカル.NETテスト | 文書手順書の修正では実行していない。 |

最初の修正文では、二重の逆引用符を使ったMarkdown記法例の `TrackerFrame` が検査に残り、全体検査が終了値123、手順書のcspellと許可一覧検査が終了値1になった。実際の識別子を通常の行内記法で示す説明に直して再検証した。失敗した記録は上書きせず `after-*`、修正後は `refined-*`、他の変更を取り込んだ後は `integrated-*` として保存した。

文書変更のため検証中心で進めた。製品実装のTDDや独立レビューを実施したとは扱わない。変更前後の本文・定義・設定を読む自己点検と、機械的な検査結果は区別している。

## 診断記録と実行条件

`reports/diagnostics/lint-guide-clarity-20260916-0826/` に、実行コマンド、終了値、検証対象HEAD、ファイル別SHA-256、実行中の内容維持、変更対応表、補助確認結果を保存した。標準出力と標準エラーは成功・失敗ともバイトを変更せず `command-logs.tar.gz` に保存し、`command-logs-manifest.json` で内容を照合できる。

共有検査器は、コピー元確認時のCodexSkill HEAD `279b8da9ffe954f78fd8461555be429f1bdcc100` からの作業用コピーを使った。各検証の内容識別一覧にコピーした検査器を含めた。npm依存物とPython仮想環境は既存環境を参照しており、完全に独立した環境での再現を証明するものではない。検証結果はこの実行環境と内容識別値に結び付く。

開始時に確認した `origin/main` の `.github/workflows/dotnet-test.yml` は、失敗時にTRX、標準出力、標準エラー、VSTest診断、MSBuild binlog、終了値、環境情報、ソースを保存する。PRのHEADツリーには同ファイルがない。今回はworkflowを変更せず、この手順書変更に関する成功・失敗の診断記録をリポジトリへ保存する。現在のPR HEADと一致しないCI結果は使用しない。

## 未完了事項と範囲外

今回の作業では、全301項目・変更前の全出現箇所の意味対応を完了していない。この手順書の修正をWR-007全体の解消とは扱わない。引用文中の未登録語について追加承認は受けておらず、許可一覧や引用文を変更しなかった。共有検査器の短い「キャプチャ」の扱いは、前報告に残る問題であり、今回は再検証・改修していない。

履歴文書の保存方法、本文側の修正、通常レビュー・独立レビューの判定はこの報告の対象外である。元レビューの重要度を変更していない。製品コードと既存の履歴報告は変更せず、mergeしていない。

## 関連する元指摘（重要度と原文を保持）

今回の修正は同種の表記問題への部分対応であり、元指摘全体を解消したという判定ではない。

### WR-007 [medium] 許可済み語を許可後も別の説明へ置き換えており、名称と本文が一致していない

- 起点: `introduced_by_change`。箇所: `Tracker/Design/Core/tracker-architecture-plan.md:596,628-634,719,752,758,854; Tracker/Tracker.DebugHost/README.md:83; Tracker/Design/DebugHost/debug-host-maintainability-design.md:14`。
- 内容: 代表例は Kalman filter → カルマン状態推定、process noise → プロセス側の揺らぎ、measurement noise → 観測側の揺らぎ、timeline scrubber → 再生位置操作部、render snapshot → 描画時点の記録。許可済みの名称を避ける必要はない。
- 影響: 推定方式、設定項目、記録形式や画面部品の名前をコード・許可一覧と結び付けにくくする。状態推定のような広い言葉では、方式名という識別情報も弱くなる。
- 根拠: whitelist:467-476 は Kalman filter / process noise / measurement noise をソース識別子付きで許可。:173-174 は timeline scrubber、:353-357 は render snapshot を許可。before/after の全登録語の出現行は同梱の audit JSON に記録。
- 必要な対応: 変更前に現れる許可済み英語・片仮名語を audit JSON から追い、同じ意味なら許可済みの標準表記・別表記を残す。まず上記5種と profile、raw vision、source frame number、Field source、replay timeline、diagnostics sample tick を本文に対応付ける。日本語の説明は名称を消す代替ではなく補足として添える。自然な日本語まで機械的に不具合とは数えず、未対応の語は個別に採否と根拠を記録する。

報告保存前に、別の実行元の報告追記2コミットも取り込み、`409d7160550914f54ec6cb0ff387a17bdf65378f` で再検証した。追記は `reports/` のみで、手順書の技術コミットは `8571c7b7bdf940414375cfb378e3a35a37eba224` のままである。

検証ごとのファイル別SHA-256一覧は `source-manifests.tar.gz` に収録し、`source-manifests-index.json` でアーカイブ内の各一覧を照合できる。元の一覧ファイルは端末上の専用作業領域にも保持する。
