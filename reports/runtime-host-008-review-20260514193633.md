# RUNTIME-HOST-008 レビュー

## Blocking findings

1. `Tracker/Tracker.RuntimeHost/RuntimeHostLifecycleService.cs:6` の `internal sealed class RuntimeHostLifecycleService` と、`IHostedService` 実装の `StartAsync` / `StopAsync` (`:8`, `:14`) に XML summary がない。今回の review scope は新規 production API / hosted service を含み、`review-enforcer` の source documentation policy は public / protected / internal API surface と重要な boundary method の XML summary を blocking rule として要求している。R008 は headless host scaffold の DI / lifecycle boundary を追加する task なので、ここが undocumented のままだと coding standard gate を満たさない。

## Non-blocking findings

- Non-blocking findings なし。

## Tests reviewed

- Worker report `reports/runtime-host-008-implementation-20260514192917.md` の証跡を確認した。
- Broad focused command は 23 passed / 1 failed。失敗は既存 `RuntimeHostDependencyBoundaryContractTests.DebugHost_ReadsLatestImmutableSnapshotOrPublishedOutputInsteadOfOwningTrackerOperationLoop` で、R008 scaffold / config scope ではなく DebugHost loop ownership marker の既存 assertion と報告されているため、R008 completion evidence の blocker にはしない。
- Adjusted R008 focused は 7 passed。対象は `RuntimeHostScaffoldContractTests` と RuntimeHost project/source boundary contract で、solution entry、default interval、configuration binding、0 以下の host start validation error、DebugHost / Blazor / diagnostics replay 非依存を確認している。
- `dotnet build Tracker/Tracker.RuntimeHost/Tracker.RuntimeHost.csproj -m:1 /nr:false` は pass、`dotnet build Tracker/Tracker.Tests/Tracker.Tests.csproj -m:1 /nr:false` は pass、`git diff --check` は pass と報告されている。親方針に従い追加 test は実行していない。

## Serena 使用有無

- Serena 使用あり。開始時に `initial_instructions` を読み、`/home/ibis/ssl/IbisDuck` を activate した。
- `find_symbol` で `RuntimeHostOptions`、`RuntimeHostServiceCollectionExtensions`、`RuntimeHostLifecycleService`、`RuntimeHostScaffoldContractTests` を確認し、`search_for_pattern` で RuntimeHost project 内の DebugHost / Blazor / diagnostics / capture / R009 実装系参照を確認した。行番号確認と diff 確認には shell の `nl -ba` / `git diff` を併用した。

## 確認事項

- R008 scope は headless project scaffold / Program / options / DI bootstrap / solution entry に留まっている。`Tracker/Tracker.RuntimeHost/Program.cs:4` から `:8` は Generic Host 起動と `AddRuntimeHost` 登録のみで、SSL-Vision input、tracker update、official packet publish normal path には踏み込んでいない。
- `RuntimeHost:OperationLoopIntervalMilliseconds` は設定として公開されている。`Tracker/Tracker.RuntimeHost/RuntimeHostOptions.cs:10` から `:21` で section 名、default 16ms、property が定義され、`Tracker/Tracker.RuntimeHost/appsettings.json:2` から `:4` に既定設定がある。
- 0 以下の validation は `Tracker/Tracker.RuntimeHost/RuntimeHostServiceCollectionExtensions.cs:21` から `:27` の `Validate(... > 0)` と `ValidateOnStart()` で host start validation error になる構造。`RuntimeHostLifecycleService.StartAsync` が `options.Value.OperationLoopIntervalMilliseconds` を読むため、host start 時に options validation が通常 path で発火する。
- RuntimeHost project は `Tracker.Core`、`Microsoft.Extensions.Hosting`、`Microsoft.Extensions.Options.ConfigurationExtensions` のみを参照している。`Tracker/Tracker.RuntimeHost/Tracker.RuntimeHost.csproj:10` から `:13`。Serena search でも RuntimeHost project 内に `Tracker.DebugHost` / Blazor / diagnostics replay UI / capture viewer / R009 の packet publish 実装系参照は見つからなかった。
- `Duck.slnx:7` に `Tracker.RuntimeHost` solution entry が追加され、`Tracker/Tracker.Tests/Tracker.Tests.csproj:21` から `:27` に test project の参照が追加されている。R008 contract tests から RuntimeHost options / DI を直接検証するための test reference として過剰ではない。
- `Directory.Packages.props:9` から `:10` の hosting / options configuration package version 追加は RuntimeHost project の package references と対応しており、過剰な UI / diagnostics package 追加はない。
- `Tracker/Design/tasks-status.md:7` から `:16` と `:119` は R008 を in-progress とし、review / commit / Draft PR #17 update が残る状態を表しているため、現実と矛盾しない。

## Final outcome

- 判定: Blocked。
- Follow-up required: あり。`RuntimeHostLifecycleService` と `StartAsync` / `StopAsync` に、headless scaffold の lifecycle / validation boundary として保持すべき contract を説明する XML summary を追加してから r2 review が必要。
