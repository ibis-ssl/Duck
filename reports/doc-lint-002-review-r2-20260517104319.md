# DOC-LINT-002 再レビュー

## 対象

- 初回レビュー指摘の修正
- SudachiPy 版 `--stdin` focused check の互換性

## レビュー結果

再レビュー済み。初回 High 指摘は解消している。

対象の `check-markdown-whitelist-sudachi.py` では、通常文書を `--stdin` で渡した場合、`read_inputs` が入力本文だけを返す分岐になっている。`tools/lint/markdown-whitelist.yaml` 自身を `--stdin` で渡した場合だけ、許可一覧の value と description を検査対象にする分岐が残っている。

## 指摘

なし。

## 確認メモ

- `check-markdown-whitelist-sudachi.py:203-207` で、通常文書の `--stdin` は `[(normalized_stdin_path, stdin_text or "")]` のみを返す。`check-markdown-whitelist-sudachi.py:214` の whitelist description 追加は、非 `--stdin` の通常ファイル選択時だけに残っている。
- `check-markdown-whitelist-sudachi.py:205-206` と `check-markdown-whitelist-sudachi.py:225-230` で、`--stdin tools/lint/markdown-whitelist.yaml` の場合は value と description を検査する特別扱いが維持されている。`is_whitelist_path` は `check-markdown-whitelist-sudachi.py:334-344` で絶対パスと `./` 付きパスも正規化している。
- 関数直接確認で、通常文書 `--stdin` は `normal: [('tools/lint/README.md', 'BODY')]`、whitelist 自身の `--stdin` は value と description の 2 入力を返すことを確認した。
- CLI 確認として、`printf 'SudachiPy\n' | PATH="$PWD/.codex-doc-lint-venv/bin:$PATH" npm run lint:md:whitelist -- --stdin tools/lint/README.md --list-unknown` は成功した。
- CLI 確認として、最小 whitelist を標準入力から `--stdin tools/lint/markdown-whitelist.yaml` へ渡すと、`ProbeDescription` が `tools/lint/markdown-whitelist.yaml:ProbeTerm [whitelist description]` の違反として検出された。特別扱いは実行時にも残っている。
- `tools/lint/README.md:7-14` で、標準の `npm run lint:md` が SudachiPy 版の許可一覧検査を実行するため `npm install` だけでは足りず、Python 仮想環境と `tools/lint/requirements.txt` の導入が必要なことを明記している。
- `reports/doc-lint-002-sudachipy-vocabulary-20260517104319.md:31-35` に、初回指摘後の `--stdin` 修正、focused check 成功、cspell/textlint/git diff check の検証が追記されている。
- `.codex-doc-lint-venv/bin/python -m py_compile .agents/skills/review-enforcer/scripts/check-markdown-whitelist-sudachi.py` と `git diff --check` は成功した。
- `reports/doc-lint-002-sudachipy-vocabulary-20260517104319.md:37-39` で、現行許可一覧が再構築前のため全体 `npm run lint:md` が失敗する状態は意図した現状として明記されている。今回の初回 High 指摘修正に対する追加ブロッカーとしては扱わない。
