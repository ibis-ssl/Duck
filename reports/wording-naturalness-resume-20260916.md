# 不自然な日本語化の追加修正

## 状態

ibis-ssl/Duck PR #20 の指摘対応を継続した。今回の本文変更は11文書、146行の変更であり、技術コミット `84dbf6c235be9e89e775bc0020fb891d56131ee5` としてpush済み。親は `9e48670560a17feb4fd65f126a6c7eac7116f7dc`。

作業全体は未完了。元の許可語301項目について全出現箇所の採否を確定したものではなく、WR-007の完了や独立レビュー合格を宣言しない。

## 主な修正

| 変更前 | 変更後・対応 |
| --- | --- |
| 設定束縛、設定の束縛 | 許可済みの「設定値のバインド」を使用し、設定値が反映される対象も明記した。 |
| 集合に参加する | 「マルチキャストグループに参加する」に変更した。 |
| キャプチャーの第一候補統合点 | 「キャプチャー処理の接続先の第一候補」に変更した。 |
| 共通の準備用オブジェクトを保持する項目の重複 | `TrackerContractFixture` の受け取りと保持を基底クラスにまとめる説明へ変更した。 |
| 意味上の概要 | 保存した検出情報と追跡結果の概要から表示を復元する説明へ変更した。 |
| tracker packet snapshot の記録の主記録 | 重複を除き「tracker packet snapshot の主記録」とした。 |
| 追跡パケット比較での「観測フレームの番号」 | 比較対象に合わせて「追跡フレームの番号」へ修正した。入力の並べ替えに使う観測番号は変更していない。 |

保存形式・識別情報の名称として、`session folder`、`snapshot sidecar`、`alignment sidecar`、`diagnostics sample sidecar`、`capture metadata`、`source key` などを許可済みの表記へ戻した。`viewport state`、`type-owned folder`、`exit code`、`camera-local track`、追跡パケットや追跡時刻の名称も同様に修正した。

変更前の用語と本文の対応は、用語整理前の `f5482ab85d49832a21ef6029d6aa3354f6c7c4f4` および既存の `reports/markdown-wording-audit-20260916.json` を参照した。機械的な行対応は候補抽出であり、それだけで語義の一致や全出現箇所の対応完了を保証しない。

箇条書きの字下げは実ファイルでは保持されていた。途中報告での「親子関係が崩れている」という説明は訂正した。字下げの修正は行っていない。

## 検証結果

| 検証 | 結果 |
| --- | --- |
| 全19文書のtextlint | 成功。 |
| 全19文書のcspell | 指摘0件。 |
| 引用文書を除く18文書の許可語検査 | 終了値0。全体成功の代用ではない。 |
| 全19文書の `npm run lint:md` | 終了値1。原文引用内の「サブエージェント」1件が残る。 |
| 文書保持の補助検証 | 58項目中58項目成功。製品テスト件数ではない。 |
| `git diff --check` | 成功。 |
| ローカル.NETテスト | 今回は実行していない。 |

補助検証は、既存のコード例とコード識別子、数値、リンク・脚注の参照先、箇条書きの字下げ、許可一覧、引用文書の保持を確認した。自然言語の意味が完全に同一であることを自動検証したものではない。

検証記録は `reports/diagnostics/wording-naturalness-resume-20260916/` に保存する。検証スクリプト、文書と検査器のSHA-256、結果JSON、成功・失敗両方の標準出力と標準エラーを含む圧縮ログを残す。最初の環境ではコピーした検査器の説明文書まで対象になったため、検査器の複製をリポジトリ外へ移し、既存と同じ参照構成に直して19文書を再検査した。製品文書を除外する設定変更は行っていない。

## 作業の分離と未完了事項

共有Pythonセッションへ別の呼び出しが入っている記録を確認したため、08:25 JST以降は `/home/ibis/.local/share/duck-naturalness-resume-0826` と短時間で終了するプロセスへ分離した。他のセッションは終了していない。先行作業の報告コミット `9e48670` も取り込み、Git操作とpushはRDC経由のgitコマンドで実行した。

WR-007の全出現箇所の採否表、WR-011の履歴本文の要約・原文保全方法の再レビューは未完了。今回の差分だけで既存の11指摘すべてが解決したとは扱わない。

原文引用を改変せず、未承認の「サブエージェント」を許可一覧へ追加していない。短い「キャプチャ」の扱いを含む共有検査器の課題も今回修正していない。承認済みの「フィールド」「設定プロファイル」「キャプチャー」は維持した。

最終報告コミットと同じHEADのCI結果は、公開後のPRコメントへ記録する。過去HEADの成功を代用しない。PRのマージ、作業の完了宣言、独立レビュー合格宣言は行っていない。

## 同時更新を取り込んだ後の確認

報告公開時に、別作業の `8571c7b` による `tools/lint/README.md` の修正が先行していたため、上書きせず取り込んだ。本報告の11文書・146行という修正範囲に、この手順書の変更は含めない。

`650783291aac10b3fdef11a7ba2acf1ff16029e6` で全19文書の検査を再実行し、引用内の未登録語1件だけが残ることを確認した。今回の11文書は当初の検証時とSHA-256が一致し、保持確認58項目と引用文書を除く18文書の用語検査も再度成功した。詳細は `integrated-validation.json` と `validation-current.json` に記録した。

同HEADの `.NET tests` / `pull_request` run `35036487242` は `completed / success`。本文修正HEAD `84dbf6c` の同workflow run `35036067099` も成功している。以後の報告・ログ保存コミットに対するCIは、そのコミットと完全一致するrunを別途PRコメントで報告する。
