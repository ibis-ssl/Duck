# フェーズ状況

ルール: このファイルは `task-breakdown-planner`、`task-consistency-manager`、`progress-sync-manager` からのみ更新する。

## 全体状況

- 現在のフェーズ: comparison-logging
- 現在のタスク: TRACKER-046
- 残りフェーズ: none

## フェーズ一覧

| フェーズ | 状態 | 完了条件 |
| --- | --- | --- |
| past-tracker-history | done | `TRACKER-000` から `TRACKER-038` までの完了済みタスクと旧フェーズ詳細は `Tracker/Tracker.Core/Design/tracker-history-000-038.md` に退避済み。tracking 軽量化と履歴退避は PR #9 準備の保守性/運用作業として完了済みで、CaptureOn 比較ログの機能仕様には含めない。 |
| investigation | done | 直近履歴として `TRACKER-039` は PR #8 `https://github.com/ibis-ssl/Duck/pull/8` で `2026-05-12T00:06:33Z` に merge 済み。証跡は `reports/tracker-039-evidence-20260512084929.md`、review は `reports/tracker-039-review-20260512085258.md` と `reports/tracker-039-review-r2-20260512090207.md` に記録済み。 |
| comparison-logging | in_progress | `TRACKER-040` は CaptureOn 比較ログ拡張の設計・tracking・draft PR #9 `https://github.com/ibis-ssl/Duck/pull/9` 更新・gpt-5.5 high review まで完了済みで、blocking findings はない。追加方針により `TRACKER-041` で保存要件としての self 除外を取り下げ、見えている `TrackerWrapperPacket` をすべて tracker packet snapshot sidecar JSONL へ保存する設計へ修正済み。ibis 自身の official packet と詳細ログの重複保持は許容し、self / 3rdparty / unknown の判別は保存除外ではなく後続表示・比較用の role / label / metadata として扱う。`TRACKER-042` では all tracker 保存 contract へテストを修正し、`MultiTrackerManager` の self early return 廃止と `TrackerState.SourceRole` / `SourceLabel` 追加により own / external / unknown tracker packet の observed / snapshot state 保持と raw payload 復元可能性を実装済み。focused test は 5 passed、full `Tracker.Tests` は 163 passed、gpt-5.5 high review は blocking findings なしで完了した。`TRACKER-043` は TDD failing test 作成後、CaptureOn session folder 配下の packet capture / metadata / tracker diagnostics / render snapshots / tracker snapshot sidecar relative path、snapshot log metadata、`TrackerPacketSnapshotRecord` / `TrackerPacketSnapshotLogReader` を production 実装済み。focused test は 5 passed、関連 focused test は 13 passed、full `Tracker.Tests` は 168 passed で、gpt-5.5 high review も blocking findings なしで完了した。snapshot が表示用データであること自体は問題ないが、比較用元データ保持は `TRACKER-044` の normal path 必須条件として扱う。source ごとの active tracker API と同一 `uuid` 衝突ケースは `TRACKER-044` の source summary / role 解決リスクとして扱い、raw payload と source identity を落とさない限り通常経路の blocker にはしない。`TRACKER-044` は TDD failing test 作成後、`TrackerPacketSnapshotLogWriter`、raw payload 付き sidecar JSONL append / flush、raw由来 `SemanticSummary`、reader 補完、metadata の `RecordCount` / `SkippedRecordCount` / `ErrorCount` / source 集計更新を production 実装済み。focused test は `TrackerComparisonSourceTddTests` 7 passed、関連 focused test は 30 passed、full `Tracker.Tests` は 175 passed。gpt-5.5 high review は `reports/tracker-044-review-20260512123921.md` に記録済みで blocking findings なし。review follow-up で `SemanticSummary` の ball / robot / tracked frame / source summary 値一致 assertion を追加し、focused test は 7 passed、関連 focused test は 30 passed。follow-up 作業レポートは `reports/tracker-044-review-followup-20260512124330.md`。`TRACKER-044` は完了。`TRACKER-045` は live 外部 tracker 受信接続の TDD failing test を作成後、`TrackerConnectionLibSnapshotRecorder` を production 実装し、`MultiTrackerManager<TrackerPacketAdapter>` の live update から `TrackerPacketSnapshotLogWriter` へ packet / source metadata を渡す接続を追加した。CaptureOn 中の TrackerConnectionLib live packet -> snapshot writer 接続、own / external / unknown 全保存、CaptureOff 中の非書き込み、CaptureOn / Off / 再On の別 session folder 化、raw payload / `SemanticSummary` の比較元データ保持は focused test 5 passed で確認済み。関連 focused test は 35 passed、full `Tracker.Tests` は 180 passed。作業レポートは `reports/tracker-045-live-receiver-implementation-20260512125847.md`、gpt-5.5 high review は `reports/tracker-045-review-20260512130623.md`、review 後同期は `reports/tracker-045-progress-sync-20260512131047.md` に記録済みで、blocking findings はない。`TRACKER-046` は runtime 起動登録の TDD failing test を作成後、production 実装として `TrackerConnectionLibReceiverHostedService`、DI 登録、uuid / sourceName 空の unknown packet を落とさない `TrackerWrapperPacketDeserializer`、handler 例外を receiver loop から隔離する `UdpTrackerReceiver` hardening を追加した。runtime 起動登録として `MultiTrackerManager<TrackerPacketAdapter>`、`TrackerConnectionLibSnapshotRecorder`、`UdpTrackerReceiver` を通常アプリ起動で常駐接続し、CaptureOn 中に実 UDP live receiver 由来の own / external / unknown tracker packet が session folder 配下の snapshot sidecar へ流れることを focused test 3 passed で確認済み。関連 focused test は 38 passed、full `Tracker.Tests` は 183 passed。gpt-5.5 high review で official multicast packet の normal path blocker が出たため、review fix TDD として `TrackerMulticastReceiverReviewFixTddTests` を追加した。official multicast group join、runtime への multicast endpoint 受け渡し、receiver 明示 enable default off、CaptureOff 中の非書き込みを固定し、production 修正で `UdpTrackerReceiver` の configured multicast group join、`Program.cs` の explicit `Tracker:Receive:Enabled` gate と multicast endpoint 受け渡し、default off 設定を実装した。multicast review fix focused test は 4 passed、関連 focused test は 42 passed、full `Tracker.Tests` は 187 passed。作業レポートは `reports/tracker-046-runtime-registration-implementation-20260512132555.md`、`reports/tracker-046-multicast-review-fix-tdd-20260512134307.md`、`reports/tracker-046-multicast-review-fix-implementation-20260512135310.md`。gpt-5.5 high re-review 待ちで、diagnostics / replay / playback UI 実装と PR ready 化は今回対象外。`Append` 直利用時の invalid raw payload hardening は通常経路外の follow-up risk として保持し、live 受信経路で必要と判明した場合だけ最小修正する。phase 完了条件は、CaptureOn 中に見えている tracker packet を session folder 配下の snapshot sidecar JSONL に保存し、metadata から各 file relative path、source identity、role、比較用 raw payload または復元可能参照を辿れ、official multicast receiver が明示有効化時だけ group join し、3rdparty tracker snapshot を `Tracker.CaptureReplay` / diagnostics / playback で再生・比較でき、既存 diagnostics log 互換性を壊さず、review / commit / PR gate が task ごとに閉じていること。 |
